# Core backend — light, fast to build, fits Render's free tier.
# Embeddings run in-process via fastembed (ONNX Runtime, NO torch): deterministic,
# no API key, no rate limits, so local and production indexes always match.
fastembed==0.3.6
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
slowapi==0.1.9
numpy==1.26.4
faiss-cpu==1.8.0
structlog==24.4.0
httpx==0.27.2

# dev / test
pytest==8.3.3
